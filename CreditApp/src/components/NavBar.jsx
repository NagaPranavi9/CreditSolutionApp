import React from 'react';

const Navbar = () => {
    return (
        <nav>
            <div>
                MJR Credit Sol
            </div>
            <div>
                <button>Login</button>
            </div>
        </nav>
    );
};

export default Navbar;
